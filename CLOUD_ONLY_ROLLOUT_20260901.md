# 雲端唯一存檔分階段方案

## 目標

資金、黃金、球探點、特訓、球員、戰績、任務與商城資料只以 Supabase 為準。本機只保存素材快取、畫面設定與未完成網路請求，不保存可直接恢復的遊戲進度。

## 階段一：安全回填（目前）

- 已建立 `godot_economy_accounts` 與伺服器流水帳。
- 已從既有存檔回填 238 筆資源帳本。
- 已建立 `godot_match_settle`，以比賽 ID 防止重複領獎。
- 舊版仍可離線遊玩，避免玩家在更新期間遺失進度。

## 階段二：新版本雙寫驗證

- 登入後先讀取雲端帳本，再比對本機顯示值。
- 比賽、任務、球探、簽約、交易、訓練全部先完成伺服器交易，再更新畫面。
- 伺服器回應逾時時不扣款、不發獎，使用同一個 `request_id` 重試。
- 連續兩個版本驗證資源帳本與舊存檔一致後，才關閉舊版直接寫入。

## 階段三：雲端唯一存檔

- 沒有登入或沒有網路時顯示連線頁，不進入可修改資源的遊戲畫面。
- 本機只留 UI 設定、下載素材快取與待處理請求 ID。
- 伺服器維護時提供唯讀公告頁，避免玩家誤以為資料消失。
- 每日備份、版本回復與帳號刪除流程完成後才全面啟用。

## 流暢度原則

- 圖片、動畫、音效依場景分批載入，不一次載入整包。
- 下載中的素材顯示骨架屏；已使用素材保留快取。
- 重要交易請求與素材下載分開，不讓大檔案阻塞資源更新。
- 低階 Android 使用 30 FPS 效能模式；中高階裝置維持 60 FPS。

## 發布閘門

以下條件全部通過後才能切換階段三：

1. Android、iOS、網頁版各完成 20 次斷線／重試測試。
2. 同一場比賽重送 10 次仍只發一次獎勵。
3. 舊玩家資源與伺服器帳本抽樣一致。
4. 伺服器備份可成功回復。
5. Apple／Google 購買收據已完成伺服器驗證。
